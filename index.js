// Create a newHeader variable and assign it to an <h1> node
const newHeader = document.createElement('h1');
newHeader.id = 'victory';
// Add any desired text or content to the newHeader element
newHeader.textContent = 'YOUR-NAME is the champion';

// Add the newHeader element to the document body
document.body.appendChild(newHeader);
